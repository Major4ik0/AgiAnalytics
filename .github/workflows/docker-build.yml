name: Build Windows Executable

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  build-win7-x64:
    name: Build Universal x64 (Win7/10/11)
    runs-on: windows-2019

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python 3.8.10 (x64)
        uses: actions/setup-python@v5
        with:
          python-version: '3.8.10'
          architecture: x64

      - name: Install dependencies for x64
        run: |
          python -m pip install --upgrade pip
          pip install numpy==1.21.6 pandas==1.3.5 PyQt5==5.15.4 pyinstaller==5.13.0 openpyxl==3.0.10 pillow==9.5.0 msoffcrypto-tool==4.12.0 olefile==0.46 xlrd==2.0.1 xlwt==1.3.0

      - name: Verify files exist
        run: |
          Write-Host "📁 Проверка файлов:"
          Get-ChildItem -Path . -Recurse -Include *.py | Select-Object -First 10 Name, FullName
          Write-Host ""
          Write-Host "📁 Проверка папки icons:"
          if (Test-Path "icons") {
            Get-ChildItem -Path "icons" | Select-Object Name, FullName
          } else {
            Write-Host "❌ Папка icons не найдена"
          }
          Write-Host ""
          Write-Host "📄 Проверка resource_helper.py:"
          if (Test-Path "resource_helper.py") {
            Write-Host "✅ resource_helper.py найден"
            Get-Content resource_helper.py -First 5
          } else {
            Write-Host "❌ resource_helper.py не найден"
            # Создаем файл на лету
            Write-Host "📝 Создаем resource_helper.py..."
            $resource_code = @'
            import sys
            import os
            
            def resource_path(relative_path):
                """Get absolute path to resource"""
                try:
                    base_path = sys._MEIPASS
                except Exception:
                    base_path = os.path.abspath(".")
                return os.path.join(base_path, relative_path)
            '@
            Set-Content -Path "resource_helper.py" -Value $resource_code -Encoding UTF8
          }

      - name: Build x64 executable
        run: |
          $exe_name = "AgiAnalytics_x64"
          Remove-Item -Recurse -Force dist, build -ErrorAction SilentlyContinue
          
          # Собираем с ресурсами (все файлы упакованы внутрь)
          pyinstaller --onefile --windowed --icon=icons/icon.ico --name="$exe_name" --noupx --clean `
            --hidden-import=PyQt5 `
            --hidden-import=PyQt5.QtCore `
            --hidden-import=PyQt5.QtGui `
            --hidden-import=PyQt5.QtWidgets `
            --hidden-import=pandas `
            --hidden-import=numpy `
            --hidden-import=numpy.core._multiarray_umath `
            --hidden-import=openpyxl `
            --hidden-import=openpyxl.cell._writer `
            --hidden-import=openpyxl.utils `
            --hidden-import=sqlite3 `
            --hidden-import=PIL `
            --hidden-import=PIL._imaging `
            --hidden-import=PIL.Image `
            --hidden-import=xlrd `
            --hidden-import=xlrd.compdoc `
            --hidden-import=xlrd.xlsx `
            --hidden-import=xlwt `
            --hidden-import=xlwt.ExcelFormula `
            --hidden-import=xlwt.ExcelFormulaParser `
            --hidden-import=msoffcrypto `
            --hidden-import=olefile `
            --hidden-import=encodings.utf_8 `
            --hidden-import=encodings.cp1251 `
            --add-data="icons/icon.ico;." `
            --add-data="icons/add_document.png;icons" `
            --add-data="icons/pencel.png;icons" `
            --add-data="icons/delete_document.png;icons" `
            --add-data="icons/logout.png;icons" `
            --add-data="icons/import.png;icons" `
            --add-data="icons/export.png;icons" `
            --add-data="icons/information.png;icons" `
            --add-data="icons/stata.png;icons" `
            --add-data="icons/settings.png;icons" `
            --add-data="icons/users.png;icons" `
            --add-data="icons/add_user.png;icons" `
            --add-data="icons/edit_user.png;icons" `
            --add-data="icons/delete_user.png;icons" `
            --add-data="icons/rules.png;icons" `
            main.py resource_helper.py
          
          if (Test-Path "dist/$exe_name.exe") {
            Write-Host "✅ Сборка x64 успешна"
            $size = (Get-Item "dist/$exe_name.exe").Length / 1MB
            Write-Host "📦 Размер файла: $($size.ToString('0.00')) MB"
            
            # Тестируем запуск
            Write-Host "🧪 Тестируем запуск..."
            $testResult = Start-Process -FilePath "dist/$exe_name.exe" -ArgumentList "--version" -NoNewWindow -Wait -PassThru
            if ($testResult.ExitCode -eq 0) {
              Write-Host "✅ Программа запускается корректно"
            }
          } else {
            Write-Error "❌ Ошибка сборки x64"
            # Показываем подробности ошибки
            if (Test-Path "build") {
              $warnFile = Get-ChildItem -Path "build" -Recurse -Filter "*.txt" | Select-Object -First 1
              if ($warnFile) {
                Write-Host "📄 Последние ошибки из warn файла:"
                Get-Content $warnFile.FullName -Tail 20
              }
            }
            exit 1
          }

      - name: Upload x64 artifact
        uses: actions/upload-artifact@v4
        with:
          name: AgiAnalytics-x64
          path: dist/AgiAnalytics_x64.exe

  build-win7-x86:
    name: Build Legacy x86 (Win7/10/11)
    runs-on: windows-2019

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python 3.8.10 (x86)
        uses: actions/setup-python@v5
        with:
          python-version: '3.8.10'
          architecture: x86

      - name: Install dependencies for x86
        run: |
          python -m pip install --upgrade pip==20.3.4
          pip install wheel==0.34.2 setuptools==44.0.0 numpy==1.19.3 pandas==1.3.5 PyQt5==5.15.4 pyinstaller==5.0.1 openpyxl==3.0.9 xlrd==2.0.1 xlwt==1.3.0 pillow==8.4.0 msoffcrypto-tool==4.12.0 olefile==0.46

      - name: Ensure resource_helper.py exists
        run: |
          # Убедимся, что файл существует
          if (-not (Test-Path "resource_helper.py")) {
            Write-Host "📝 Создаем resource_helper.py..."
            $resource_code = @'
            import sys
            import os
            
            def resource_path(relative_path):
                """Get absolute path to resource"""
                try:
                    base_path = sys._MEIPASS
                except Exception:
                    base_path = os.path.abspath(".")
                return os.path.join(base_path, relative_path)
            
            def get_icon_path(icon_name="icon.ico"):
                """Get icon path"""
                icon_paths = [
                    icon_name,
                    f"icons/{icon_name}",
                    f"../icons/{icon_name}",
                ]
                
                for icon_path in icon_paths:
                    full_path = resource_path(icon_path)
                    if os.path.exists(full_path):
                        return full_path
                
                return None
            '@
            Set-Content -Path "resource_helper.py" -Value $resource_code -Encoding UTF8
            Write-Host "✅ resource_helper.py создан"
          } else {
            Write-Host "✅ resource_helper.py уже существует"
          }

      - name: Build x86 executable
        run: |
          $exe_name = "AgiAnalytics_x86"
          Remove-Item -Recurse -Force dist, build -ErrorAction SilentlyContinue
          
          # Простая сборка без лишних hidden-imports
          pyinstaller --onefile --windowed --icon=icons/icon.ico --name="$exe_name" --noupx --clean `
            --add-data="icons/icon.ico;." `
            --add-data="icons/add_document.png;icons" `
            --add-data="icons/pencel.png;icons" `
            --add-data="icons/delete_document.png;icons" `
            --add-data="icons/logout.png;icons" `
            --add-data="icons/import.png;icons" `
            --add-data="icons/export.png;icons" `
            --add-data="icons/information.png;icons" `
            --add-data="icons/stata.png;icons" `
            --add-data="icons/settings.png;icons" `
            --add-data="icons/users.png;icons" `
            --add-data="icons/add_user.png;icons" `
            --add-data="icons/edit_user.png;icons" `
            --add-data="icons/delete_user.png;icons" `
            --add-data="icons/rules.png;icons" `
            main.py resource_helper.py
          
          if (Test-Path "dist/$exe_name.exe") {
            Write-Host "✅ Сборка x86 успешна"
            $size = (Get-Item "dist/$exe_name.exe").Length / 1MB
            Write-Host "📦 Размер файла: $($size.ToString('0.00')) MB"
          } else {
            Write-Error "❌ Ошибка сборки x86"
            exit 1
          }

      - name: Upload x86 artifact
        uses: actions/upload-artifact@v4
        with:
          name: AgiAnalytics-x86
          path: dist/AgiAnalytics_x86.exe

  create-release:
    name: Create Universal Release
    needs: [build-win7-x64, build-win7-x86]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write

    steps:
      - name: Get current date
        id: date
        run: echo "date=$(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_OUTPUT

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release structure
        run: |
          version=${{ github.run_number }}
          release_name="AgiAnalytics_v$version"
          
          # Создаем структуру каталогов
          mkdir -p "$release_name/x64"
          mkdir -p "$release_name/x86"
          
          # Копируем исполняемые файлы
          if [ -f "artifacts/AgiAnalytics-x64/AgiAnalytics_x64.exe" ]; then
            cp "artifacts/AgiAnalytics-x64/AgiAnalytics_x64.exe" "$release_name/x64/"
            echo "✅ x64 версия скопирована"
          fi
          
          if [ -f "artifacts/AgiAnalytics-x86/AgiAnalytics_x86.exe" ]; then
            cp "artifacts/AgiAnalytics-x86/AgiAnalytics_x86.exe" "$release_name/x86/"
            echo "✅ x86 версия скопирована"
          fi
          
          # Создаем автоматический лаунчер
          cat > "$release_name/Start_AgiAnalytics.bat" << 'EOF'
          @echo off
          chcp 65001 >nul
          echo ========================================
          echo   AgiAnalytics - Автоматический запуск
          echo ========================================
          echo.
          
          REM Определяем архитектуру
          if "%PROCESSOR_ARCHITECTURE%"=="AMD64" (
              set "EXE_PATH=x64\AgiAnalytics_x64.exe"
              echo Обнаружена 64-битная система
          ) else (
              set "EXE_PATH=x86\AgiAnalytics_x86.exe"
              echo Обнаружена 32-битная система
          )
          
          echo.
          
          REM Проверяем существование файла
          if exist "%EXE_PATH%" (
              echo Запуск программы...
              echo.
              start "" "%EXE_PATH%"
          ) else (
              echo ❌ Ошибка: Файл не найден
              echo Путь: %EXE_PATH%
              echo.
              echo Проверьте, что:
              echo 1. Вы распаковали весь архив
              echo 2. Файлы находятся в правильных папках
              echo.
              pause
          )
          EOF
                    
                    # Создаем README
                    cat > "$release_name/README.txt" << EOF
          ========================================
          AgiAnalytics v$version
          ========================================
          
          СОДЕРЖИМОЕ:
          • x64/AgiAnalytics_x64.exe - 64-битная версия
          • x86/AgiAnalytics_x86.exe - 32-битная версия
          • Start_AgiAnalytics.bat   - Автозапуск
          
          🚀 ИНСТРУКЦИЯ:
          1. Распакуйте архив в любую папку
          2. Запустите Start_AgiAnalytics.bat
          3. Программа сама выберет нужную версию
          
          ⚡ ОСОБЕННОСТИ:
          • Все ресурсы упакованы внутрь EXE файла
          • Не требует дополнительных файлов
          • Автономная работа
          
          ⚠️  ДЛЯ WINDOWS 7:
          Если программа не запускается:
          1. Установите обновление KB2999226
          2. Установите Visual C++ Redistributable
          3. Перезагрузите компьютер
          
          📞 ПОДДЕРЖКА:
          https://github.com/${{ github.repository }}/issues
          
          📅 Дата сборки: ${{ steps.date.outputs.date }}
          EOF

      - name: Create ZIP archive
        run: |
          version=${{ github.run_number }}
          release_name="AgiAnalytics_v$version"
          zip -r "${release_name}.zip" "$release_name"
          echo "✅ Архив создан: ${release_name}.zip"
          echo "📊 Размер архива: $(du -h "${release_name}.zip" | cut -f1)"

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: AgiAnalytics-Release
          path: AgiAnalytics_v${{ github.run_number }}.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          files: AgiAnalytics_v${{ github.run_number }}.zip
          tag_name: v${{ github.run_number }}
          name: AgiAnalytics v${{ github.run_number }}
          body: |
            ## 🚀 Универсальная сборка для Windows
            
            ### 📦 Содержимое:
            - **x64/AgiAnalytics_x64.exe** - для 64-битных систем
            - **x86/AgiAnalytics_x86.exe** - для 32-битных систем
            - **Start_AgiAnalytics.bat** - автоматический выбор версии
            
            ### ⚡ Особенности:
            - ✅ Все ресурсы упакованы внутрь EXE
            - ✅ Не требует дополнительных файлов
            - ✅ Автономная работа
            - ✅ Иконки включены в исполняемый файл
            
            ### 🚀 Быстрый старт:
            1. Скачайте архив
            2. Распакуйте в любую папку
            3. Запустите `Start_AgiAnalytics.bat`
            
            ### 🔧 Системные требования:
            - Windows 7 SP1 или новее
            - 2 ГБ ОЗУ (рекомендуется 4 ГБ)
            - 100 МБ свободного места
            
            ### 🛠️ Для Windows 7:
            Если программа не запускается, установите:
            1. [Обновление KB2999226](https://support.microsoft.com/en-us/topic/update-for-universal-c-runtime-in-windows-c0514201-7fe6-95a3-b0a5-287930f3560c)
            2. [Visual C++ Redistributable](https://aka.ms/vs/17/release/vc_redist.x64.exe)
            
            ### 📝 Информация о сборке:
            - Версия Python: 3.8.10
            - PyInstaller: 5.13.0 (x64) / 5.0.1 (x86)
            - Сборка №: ${{ github.run_number }}
            - Дата сборки: ${{ steps.date.outputs.date }}
            
            ### 🔗 Ссылки:
            - [Исходный код](https://github.com/${{ github.repository }})
            - [Сообщить об ошибке](https://github.com/${{ github.repository }}/issues)
            
            ---
            *Собрано: ${{ steps.date.outputs.date }}*
          draft: false
          prerelease: false